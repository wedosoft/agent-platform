# Freshworks 제품군 학습 모델 시스템 재설계 지침서
**Version 1.0 | 2025-12-03**

---

## 📋 Executive Summary

본 문서는 Freshworks 제품군의 복잡한 구조를 효과적으로 학습하고 추론할 수 있는 AI 학습 모델 시스템의 재설계 지침을 제공합니다. 현재의 계층적 분류 방식에서 벗어나 **모듈 기반 조합 아키텍처**로 전환하여, 제품 간 복잡한 의존성과 동적 조합 패턴을 정확하게 표현하고 미래 확장성을 확보합니다.

**핵심 목표:**
- 독립 제품, 통합 번들, 애드온의 관계를 명확히 모델링
- 동적 제품 조합 시나리오 지원
- Freshworks의 통합 플랫폼 진화 방향 선제 대응
- 확장 가능하고 유지보수 용이한 구조 설계

---

## 🎯 1. 현재 시스템의 문제점 분석

### 1.1 구조적 문제
```
❌ 문제: 계층적 트리 구조로 제품 분류 시도
→ Freshdesk Omni = Freshdesk + Freshchat 관계 표현 불가
→ Add-on 개념이 별도 카테고리로 분리되어 혼란

❌ 문제: 제품 간 의존성과 제약 조건 미반영
→ Freshsales Add-on의 최소/최대 라이선스 수 규칙 누락
→ 플랜 레벨 매칭 요구사항 표현 안 됨

❌ 문제: Static 분류 방식
→ 비즈니스 시나리오별 동적 조합 추천 불가
→ 신규 제품 추가 시 전체 구조 재설계 필요
```

### 1.2 학습 효율성 문제
- 제품 간 중복 기능에 대한 중복 학습 발생
- 통합 번들의 시너지 효과 학습 불가능
- 크로스 도메인 통합 패턴 이해도 낮음

---

## 🏗️ 2. 재설계 핵심 원칙

### 2.1 Design Principles

#### **원칙 1: Composition Over Hierarchy (조합 우선)**
```
계층 구조 → 모듈 조합 구조
• 각 제품을 독립적인 기능 모듈로 정의
• 번들 제품을 "Base Module + Integration Layer"로 표현
• 동적 조합 규칙을 메타데이터로 관리
```

#### **원칙 2: Domain-Driven Design (도메인 중심)**
```
제품 이름 → 비즈니스 도메인
• Customer Service Domain
• Sales & Marketing Domain  
• IT Service Management Domain
• 도메인 간 통합 패턴을 명시적으로 정의
```

#### **원칙 3: Constraint-Aware Architecture (제약 인식)**
```
단순 관계 → 제약 조건 포함 관계
• 라이선스 수량 제약
• 플랜 레벨 매칭 규칙
• 역할 기반 접근 제어
• 메타데이터로 선언적 정의
```

#### **원칙 4: Future-Proof Flexibility (미래 대응)**
```
현재 구조 고정 → 진화 가능 구조
• Unified Workspace 개념 선제 반영
• AI 통합 레이어 고려 (Freddy AI 등)
• 신규 제품/기능 추가 시 구조 변경 최소화
```

---

## 💾 3. 데이터 모델 설계

### 3.1 Core Entities

#### **Entity 1: Product Module (제품 모듈)**
```typescript
interface ProductModule {
  id: string;                    // "freshdesk", "freshchat", "freshsales"
  name: string;
  domain: Domain;                // "customer_service", "sales_marketing", "itsm"
  capabilities: Capability[];    // 핵심 기능 목록
  standalone: boolean;           // 독립 사용 가능 여부
  pricing: PricingInfo;
  metadata: Record<string, any>;
}

enum Domain {
  CUSTOMER_SERVICE = "customer_service",
  SALES_MARKETING = "sales_marketing", 
  ITSM = "itsm"
}

interface Capability {
  id: string;                    // "ticketing", "chat", "voice", "crm"
  description: string;
  freddy_ai_enabled: boolean;    // AI 기능 지원 여부
}
```

#### **Entity 2: Product Bundle (통합 번들)**
```typescript
interface ProductBundle {
  id: string;                    // "freshdesk_omni", "freshsales_suite"
  name: string;
  base_modules: string[];        // 기본 구성 모듈 ID
  integration_type: IntegrationType;
  available_addons: AddonConfig[];
  workspace_type: "unified" | "integrated" | "connected";
  pricing_advantage: number;     // 번들 할인율 (20-25%)
}

enum IntegrationType {
  NATIVE_UNIFIED = "native",     // Freshdesk Omni 스타일
  CONNECTED_SUITE = "suite"      // Freshsales Suite 스타일
}
```

#### **Entity 3: Addon Configuration (애드온 설정)**
```typescript
interface AddonConfig {
  addon_module_id: string;       // "freshsales", "freshcaller"
  target_bundle_id: string;      // "freshdesk_omni"
  constraints: Constraint[];
  integration_depth: "deep" | "shared" | "lightweight";
}

interface Constraint {
  type: "license_quantity" | "plan_tier" | "role_assignment";
  rule: ConstraintRule;
}

interface ConstraintRule {
  min_seats?: string;            // "num_of_admins"
  max_seats?: string;            // "css_plan_licenses"
  plan_matching?: "exact" | "minimum";
  expression?: string;           // 복잡한 규칙은 표현식으로
}
```

#### **Entity 4: Business Scenario (비즈니스 시나리오)**
```typescript
interface BusinessScenario {
  id: string;
  name: string;
  description: string;
  company_profile: CompanyProfile;
  recommended_composition: ProductComposition;
  rationale: string;
}

interface CompanyProfile {
  size: "small" | "medium" | "enterprise";
  primary_focus: Domain;
  growth_stage: "startup" | "growing" | "established";
  complexity_level: 1 | 2 | 3 | 4;
}

interface ProductComposition {
  core_products: string[];       // 필수 제품
  optional_addons: string[];     // 선택적 애드온
  estimated_cost: number;
  implementation_complexity: number;
}
```

### 3.2 Relationship Graph

```typescript
interface ProductRelationship {
  source_id: string;
  target_id: string;
  relationship_type: RelationType;
  bidirectional: boolean;
  constraints?: Constraint[];
}

enum RelationType {
  BUNDLES_WITH = "bundles_with",         // Freshdesk + Freshchat → Omni
  ADDON_TO = "addon_to",                 // Freshsales → Freshdesk Omni
  SHARES_WITH = "shares_with",           // Freshcaller ↔ Omni/CRM
  INTEGRATES_WITH = "integrates_with",   // API 기반 통합
  REPLACES = "replaces"                  // 상위 플랜으로 대체
}
```

---

## 🔌 4. API 및 Query Interface 설계

### 4.1 Core Query APIs

#### **API 1: Product Discovery**
```typescript
// 특정 니즈에 맞는 제품 검색
POST /api/v2/products/discover
{
  "requirements": {
    "domains": ["customer_service"],
    "capabilities": ["ticketing", "chat", "ai_chatbot"],
    "budget_range": [1000, 5000],
    "team_size": 50
  }
}

Response:
{
  "recommendations": [
    {
      "composition": {
        "bundle": "freshdesk_omni",
        "addons": []
      },
      "match_score": 0.95,
      "rationale": "Unified omnichannel solution with native chat integration",
      "estimated_cost": 1450
    }
  ]
}
```

#### **API 2: Composition Validation**
```typescript
// 제품 조합의 유효성 검증
POST /api/v2/products/validate-composition
{
  "composition": {
    "base": "freshdesk_omni",
    "addons": [
      {
        "module": "freshsales",
        "plan_tier": "pro",
        "seats": 30
      }
    ]
  },
  "context": {
    "current_plan": "freshdesk_omni_pro",
    "total_licenses": 50,
    "admin_count": 5
  }
}

Response:
{
  "valid": true,
  "constraints_satisfied": true,
  "violations": [],
  "warnings": [
    "Consider upgrading to Enterprise plan for advanced features"
  ]
}
```

#### **API 3: Scenario-Based Recommendation**
```typescript
// 시나리오 기반 제품 추천
POST /api/v2/products/recommend-by-scenario
{
  "scenario": {
    "company_size": "growing",
    "primary_goal": "improve_customer_support",
    "secondary_goal": "enable_sales_team",
    "current_tools": ["zendesk", "hubspot"],
    "pain_points": ["fragmented_data", "high_cost"]
  }
}

Response:
{
  "migration_path": {
    "phase1": {
      "products": ["freshdesk_omni"],
      "rationale": "Replace Zendesk with unified support platform",
      "timeline": "1-2 months"
    },
    "phase2": {
      "products": ["freshdesk_omni", "freshsales_addon"],
      "rationale": "Add sales capability while maintaining support context",
      "timeline": "3-4 months"
    }
  }
}
```

### 4.2 Learning Model Queries

#### **Query 1: Feature Similarity**
```typescript
// 제품 간 기능 유사도 계산
GET /api/v2/learning/feature-similarity?product1=freshdesk&product2=freshservice

Response:
{
  "similarity_score": 0.72,
  "common_capabilities": ["ticketing", "automation", "reporting"],
  "unique_to_product1": ["customer_portal", "satisfaction_surveys"],
  "unique_to_product2": ["asset_management", "change_management"]
}
```

#### **Query 2: Integration Pattern Learning**
```typescript
// 통합 패턴 학습 데이터 조회
GET /api/v2/learning/integration-patterns?bundle=freshdesk_omni

Response:
{
  "pattern_type": "native_unified",
  "integration_points": [
    {
      "source": "freshchat",
      "target": "freshdesk",
      "method": "conversation_to_ticket",
      "context_preservation": true,
      "data_flow": "bidirectional"
    }
  ],
  "workspace_characteristics": {
    "single_agent_view": true,
    "unified_reporting": true,
    "shared_contact_database": true
  }
}
```

---

## 🚀 5. 구현 로드맵

### Phase 1: Foundation (2-3주)
**목표: 새로운 데이터 모델 구축**

```
Week 1-2: 데이터 모델 구현
□ ProductModule, ProductBundle, AddonConfig 엔티티 정의
□ Relationship Graph 구조 설계
□ Constraint 시스템 구현
□ 마이그레이션 스크립트 작성

Week 3: 기본 데이터 입력
□ 11개 주요 제품 모듈 데이터 입력
□ Freshdesk Omni, Freshsales Suite 번들 정의
□ Add-on 제약 조건 설정
□ 관계 그래프 구축
```

**기술 스택 권장:**
- Graph Database: Neo4j (관계 쿼리 최적화)
- Document Store: MongoDB (유연한 메타데이터)
- Cache Layer: Redis (빠른 조합 검증)

### Phase 2: Query Engine (3-4주)
**목표: 핵심 조회 API 구현**

```
Week 4-5: API 개발
□ Product Discovery API
□ Composition Validation API
□ Scenario Recommendation API
□ Feature Similarity Query

Week 6-7: 학습 모델 통합
□ 제품 특징 벡터화 (Embedding)
□ 유사도 계산 알고리즘
□ 시나리오 매칭 모델 훈련
□ 추천 엔진 구축
```

**ML Framework 권장:**
- Sentence Transformers (제품 설명 임베딩)
- LightGBM (시나리오 매칭)
- Graph Neural Network (관계 학습)

### Phase 3: Intelligence Layer (4-6주)
**목표: AI 추론 및 추천 기능**

```
Week 8-10: 고급 기능 개발
□ 비용 최적화 엔진
□ 마이그레이션 경로 생성기
□ 라이선스 최적화 분석
□ What-if 시뮬레이션

Week 11-13: 통합 및 테스트
□ 기존 시스템과 통합
□ 엔드투엔드 테스트
□ 성능 최적화
□ 문서화
```

### Phase 4: Future-Proofing (지속적)
**목표: 확장성 및 유지보수**

```
Ongoing:
□ 신규 제품 추가 프로세스 자동화
□ Freshworks API 변경사항 자동 감지
□ 학습 모델 주기적 재훈련
□ A/B 테스트 기반 추천 개선
```

---

## 📊 6. 데이터 구조 예시

### 6.1 실제 데이터 샘플

```json
{
  "product_modules": [
    {
      "id": "freshdesk",
      "name": "Freshdesk",
      "domain": "customer_service",
      "capabilities": [
        {
          "id": "ticketing",
          "description": "Multi-channel ticket management",
          "freddy_ai_enabled": true
        },
        {
          "id": "collaboration",
          "description": "Team collaboration tools",
          "freddy_ai_enabled": false
        },
        {
          "id": "automation",
          "description": "Workflow automation",
          "freddy_ai_enabled": true
        }
      ],
      "standalone": true,
      "pricing": {
        "free_tier": true,
        "starting_price": 15,
        "currency": "USD",
        "billing_unit": "agent/month"
      }
    },
    {
      "id": "freshchat",
      "name": "Freshchat",
      "domain": "customer_service",
      "capabilities": [
        {
          "id": "live_chat",
          "description": "Real-time customer chat",
          "freddy_ai_enabled": true
        },
        {
          "id": "chatbot",
          "description": "AI-powered chatbot",
          "freddy_ai_enabled": true
        }
      ],
      "standalone": true,
      "pricing": {
        "free_tier": true,
        "starting_price": 19,
        "currency": "USD",
        "billing_unit": "agent/month"
      }
    }
  ],
  
  "product_bundles": [
    {
      "id": "freshdesk_omni",
      "name": "Freshdesk Omni",
      "base_modules": ["freshdesk", "freshchat"],
      "integration_type": "native",
      "workspace_type": "unified",
      "available_addons": [
        {
          "addon_module_id": "freshsales",
          "integration_depth": "deep",
          "constraints": [
            {
              "type": "license_quantity",
              "rule": {
                "min_seats": "num_of_admins",
                "max_seats": "css_plan_licenses",
                "expression": "addon_seats <= base_licenses && addon_seats >= admin_count"
              }
            },
            {
              "type": "plan_tier",
              "rule": {
                "plan_matching": "exact",
                "expression": "addon_plan_tier == base_plan_tier"
              }
            }
          ]
        },
        {
          "addon_module_id": "freshcaller",
          "integration_depth": "shared",
          "constraints": []
        }
      ],
      "pricing": {
        "starting_price": 29,
        "currency": "USD",
        "billing_unit": "agent/month",
        "savings_vs_separate": 0.25
      },
      "value_propositions": [
        "Single agent workspace for all channels",
        "Unified customer context",
        "No app switching required",
        "Consolidated reporting"
      ]
    }
  ],
  
  "business_scenarios": [
    {
      "id": "scenario_growing_support_team",
      "name": "Growing Support Team with Sales Aspirations",
      "description": "Mid-size company expanding support while building sales capability",
      "company_profile": {
        "size": "medium",
        "team_size_range": [20, 100],
        "primary_focus": "customer_service",
        "growth_stage": "growing",
        "complexity_level": 3
      },
      "recommended_composition": {
        "phase1": {
          "core_products": ["freshdesk_omni"],
          "optional_addons": [],
          "rationale": "Start with unified support platform",
          "estimated_cost": 1450
        },
        "phase2": {
          "core_products": ["freshdesk_omni"],
          "optional_addons": ["freshsales"],
          "rationale": "Add sales without losing support context",
          "estimated_cost": 2900
        }
      },
      "success_metrics": [
        "First response time < 2 hours",
        "CSAT score > 90%",
        "Sales conversion rate > 15%"
      ]
    }
  ],
  
  "relationship_graph": {
    "edges": [
      {
        "source_id": "freshdesk",
        "target_id": "freshchat",
        "relationship_type": "bundles_with",
        "result_bundle_id": "freshdesk_omni",
        "bidirectional": false,
        "integration_characteristics": {
          "conversation_to_ticket": true,
          "shared_contact_db": true,
          "unified_workspace": true,
          "context_preservation": "full"
        }
      },
      {
        "source_id": "freshsales",
        "target_id": "freshdesk_omni",
        "relationship_type": "addon_to",
        "bidirectional": false,
        "constraints": [
          {
            "type": "license_quantity",
            "rule": {
              "min_seats": "num_of_admins",
              "max_seats": "css_plan_licenses"
            }
          }
        ]
      },
      {
        "source_id": "freshcaller",
        "target_id": "freshdesk_omni",
        "relationship_type": "shares_with",
        "bidirectional": true,
        "integration_characteristics": {
          "call_logging": true,
          "ticket_creation": true,
          "shared_usage": "dual_purpose"
        }
      }
    ]
  }
}
```

---

## 🎓 7. 학습 모델 훈련 전략

### 7.1 Training Data Preparation

```python
# 제품 특징 벡터화
from sentence_transformers import SentenceTransformer

model = SentenceTransformer('all-MiniLM-L6-v2')

def create_product_embedding(product: ProductModule):
    """제품의 텍스트 설명을 벡터로 변환"""
    description = f"{product.name} {product.domain} "
    description += " ".join([cap.description for cap in product.capabilities])
    return model.encode(description)

# 관계 그래프 임베딩
import networkx as nx
from node2vec import Node2Vec

def create_relationship_graph():
    """제품 관계를 그래프로 구조화"""
    G = nx.DiGraph()
    for edge in relationship_graph['edges']:
        G.add_edge(
            edge['source_id'], 
            edge['target_id'],
            type=edge['relationship_type'],
            weight=calculate_edge_weight(edge)
        )
    return G

def train_graph_embeddings(G):
    """그래프 구조 학습"""
    node2vec = Node2Vec(G, dimensions=64, walk_length=10, num_walks=100)
    model = node2vec.fit(window=5, min_count=1)
    return model
```

### 7.2 Recommendation Model

```python
from lightgbm import LGBMRanker

class ProductRecommender:
    def __init__(self, product_embeddings, graph_embeddings):
        self.product_embeddings = product_embeddings
        self.graph_embeddings = graph_embeddings
        self.ranker = LGBMRanker()
        
    def prepare_features(self, scenario: BusinessScenario, product: ProductModule):
        """시나리오와 제품의 특징 결합"""
        features = np.concatenate([
            self.encode_scenario(scenario),
            self.product_embeddings[product.id],
            self.graph_embeddings.wv[product.id],
            self.calculate_compatibility_score(scenario, product)
        ])
        return features
    
    def train(self, training_scenarios, labels):
        """과거 성공/실패 사례로 학습"""
        X = []
        y = []
        groups = []
        
        for scenario, selected_products, success_score in training_scenarios:
            scenario_features = []
            for product in all_products:
                features = self.prepare_features(scenario, product)
                scenario_features.append(features)
                y.append(1 if product in selected_products else 0)
            
            X.extend(scenario_features)
            groups.append(len(scenario_features))
        
        self.ranker.fit(X, y, group=groups)
    
    def recommend(self, scenario: BusinessScenario, top_k=5):
        """시나리오에 최적 제품 추천"""
        candidates = []
        for product in all_products:
            features = self.prepare_features(scenario, product)
            score = self.ranker.predict([features])[0]
            candidates.append((product, score))
        
        # 제약 조건 필터링
        valid_candidates = self.filter_by_constraints(candidates)
        
        # 조합 최적화
        optimal_composition = self.optimize_composition(
            valid_candidates, 
            scenario.budget,
            scenario.priorities
        )
        
        return optimal_composition[:top_k]
```

### 7.3 Constraint Satisfaction Engine

```python
from constraint import Problem, MinSizeConstraint, MaxSizeConstraint

class CompositionValidator:
    def __init__(self, constraint_rules):
        self.rules = constraint_rules
        
    def validate(self, composition: ProductComposition, context: dict):
        """제품 조합의 유효성 검증"""
        problem = Problem()
        
        # 변수 정의
        for addon in composition.addons:
            problem.addVariable(
                f"{addon.module_id}_seats",
                range(1, context['total_licenses'] + 1)
            )
        
        # 제약 조건 추가
        for addon in composition.addons:
            constraints = self.get_constraints(
                composition.base, 
                addon.module_id
            )
            
            for constraint in constraints:
                if constraint.type == "license_quantity":
                    min_val = self.evaluate_expression(
                        constraint.rule.min_seats, 
                        context
                    )
                    max_val = self.evaluate_expression(
                        constraint.rule.max_seats,
                        context
                    )
                    problem.addConstraint(
                        lambda seats: min_val <= seats <= max_val,
                        (f"{addon.module_id}_seats",)
                    )
        
        solutions = problem.getSolutions()
        return len(solutions) > 0, solutions
    
    def evaluate_expression(self, expr: str, context: dict):
        """동적 표현식 평가"""
        if expr == "num_of_admins":
            return context['admin_count']
        elif expr == "css_plan_licenses":
            return context['total_licenses']
        else:
            # 복잡한 표현식은 safe eval 사용
            return safe_eval(expr, context)
```

---

## 📈 8. 성공 지표 (Success Metrics)

### 8.1 시스템 성능 지표

```yaml
Technical Metrics:
  - Query Response Time: < 200ms (95th percentile)
  - Composition Validation: < 50ms
  - Recommendation Accuracy: > 85% (based on user acceptance)
  - Graph Traversal Performance: < 100ms for 3-hop queries
  - Cache Hit Rate: > 80%

Scalability Metrics:
  - Support for 50+ products without performance degradation
  - Handle 10,000+ concurrent queries
  - New product addition time: < 2 hours
  - Constraint update propagation: < 5 minutes
```

### 8.2 비즈니스 가치 지표

```yaml
Business Impact:
  - Recommendation Acceptance Rate: > 70%
  - Time to Product Selection: Reduce by 50%
  - Configuration Error Rate: < 5%
  - Customer Satisfaction (Product Fit): > 4.5/5
  - Sales Cycle Reduction: 20-30%

Model Quality:
  - Feature Coverage: > 95% of product capabilities
  - Relationship Accuracy: 100% (verified against official docs)
  - Constraint Satisfaction: 100% (no invalid combinations)
  - Scenario Coverage: 20+ common business scenarios
```

---

## 🔄 9. 유지보수 및 업데이트 프로세스

### 9.1 자동 모니터링

```python
class FreshworksChangeDetector:
    """Freshworks 공식 문서/API 변경 감지"""
    
    def __init__(self):
        self.watched_urls = [
            "https://www.freshworks.com/products/",
            "https://www.freshworks.com/freshdesk/omni/pricing/",
            "https://developers.freshworks.com/api/"
        ]
    
    async def monitor_changes(self):
        """일일 변경사항 체크"""
        for url in self.watched_urls:
            current_content = await fetch_content(url)
            previous_hash = self.get_cached_hash(url)
            current_hash = hash_content(current_content)
            
            if current_hash != previous_hash:
                changes = self.detect_changes(url, current_content)
                await self.notify_team(changes)
                await self.suggest_updates(changes)
    
    def detect_changes(self, url, content):
        """구조화된 변경사항 분석"""
        changes = {
            'new_products': [],
            'pricing_changes': [],
            'feature_updates': [],
            'deprecated_items': []
        }
        
        # AI 기반 변경사항 분석
        analysis = analyze_with_llm(content, self.previous_versions[url])
        changes.update(analysis)
        
        return changes
```

### 9.2 버전 관리 전략

```yaml
Data Versioning:
  - Product data: Semantic versioning (e.g., 1.2.3)
  - Schema changes: Migration scripts with rollback capability
  - Model versions: Track training data version + model architecture
  
Update Process:
  1. Detect change in Freshworks documentation
  2. Create staging branch with proposed updates
  3. Run validation tests (constraint satisfaction, query regression)
  4. A/B test recommendations (10% traffic)
  5. Full rollout if metrics improve
  
Rollback Plan:
  - Keep last 3 versions of product data
  - Automated rollback triggers on error rate spike
  - Manual rollback option for quality issues
```

---

## 🚧 10. 마이그레이션 계획

### 10.1 기존 시스템에서 전환

```yaml
Phase 1: Parallel Running (2 weeks)
  - 새 시스템 배포 (read-only mode)
  - 기존 시스템과 결과 비교
  - 불일치 항목 분석 및 수정

Phase 2: Shadow Mode (2 weeks)
  - 새 시스템에서 추천 생성
  - A/B 테스트 (50/50 트래픽)
  - 성능 및 정확도 모니터링

Phase 3: Full Migration (1 week)
  - 새 시스템으로 완전 전환
  - 기존 시스템은 fallback으로 유지
  - 1주일 모니터링 후 구 시스템 종료

Data Migration:
  - 기존 제품 카테고리 → ProductModule 매핑
  - 수동 분류 규칙 → Constraint 변환
  - 과거 추천 기록 → 학습 데이터 변환
```

### 10.2 리스크 관리

```yaml
Risk 1: 데이터 불일치
  Mitigation: 
    - Freshworks 공식 문서 우선 (single source of truth)
    - 주간 정합성 체크 자동화
    - 불일치 발견 시 즉시 알림

Risk 2: 성능 저하
  Mitigation:
    - 다층 캐싱 전략 (Redis + CDN)
    - 쿼리 최적화 (인덱싱, 구체화된 뷰)
    - 수평 확장 준비 (샤딩 전략)

Risk 3: 추천 품질 저하
  Mitigation:
    - 실시간 피드백 수집
    - 주간 모델 재훈련
    - 인간 검증 프로세스 (샘플링)
```

---

## 📚 11. 참고 자료 및 도구

### 11.1 추천 기술 스택

```yaml
Backend:
  - Language: Python 3.11+ (ML/AI 생태계)
  - API Framework: FastAPI (high performance, async)
  - Graph Database: Neo4j 5.x (관계 쿼리)
  - Document Store: MongoDB 7.x (유연한 스키마)
  - Cache: Redis 7.x (고속 액세스)
  - Message Queue: RabbitMQ (비동기 업데이트)

Machine Learning:
  - Embeddings: Sentence-Transformers
  - Ranking: LightGBM / XGBoost
  - Graph Learning: PyTorch Geometric
  - Constraint Solving: python-constraint
  - Model Serving: TorchServe / TensorFlow Serving

DevOps:
  - Containerization: Docker + Kubernetes
  - CI/CD: GitHub Actions
  - Monitoring: Prometheus + Grafana
  - Logging: ELK Stack
  - APM: DataDog / New Relic
```

### 11.2 학습 리소스

```markdown
## 필독 자료
- [Neo4j Graph Data Science](https://neo4j.com/docs/graph-data-science/)
- [LightGBM Ranking Tutorial](https://lightgbm.readthedocs.io/en/latest/pythonapi/lightgbm.LGBMRanker.html)
- [Constraint Satisfaction Problems](https://artint.info/2e/html/ArtInt2e.Ch4.S1.html)

## Freshworks 공식 문서
- [Freshworks Developer Portal](https://developers.freshworks.com/)
- [Freshdesk Omni Documentation](https://support.freshdesk.com/support/solutions/folders/50000011785)
- [Freshsales Add-on Guide](https://crmsupport.freshworks.com/support/solutions/articles/50000009560)

## 코드 참고
- Graph Embeddings: https://github.com/eliorc/node2vec
- Product Recommendations: https://github.com/microsoft/recommenders
```

---

## ✅ 12. 체크리스트 및 다음 단계

### 개발 팀 Action Items

```markdown
## Immediate (이번 주)
- [ ] 본 지침서 팀 전체 리뷰 세션 (2시간)
- [ ] 기술 스택 최종 결정 회의
- [ ] 개발 환경 설정 (Neo4j, MongoDB, Redis)
- [ ] Git Repository 구조 설계
- [ ] Phase 1 태스크 세부 분해

## Short-term (2주 내)
- [ ] ProductModule 엔티티 코드 구현
- [ ] 샘플 데이터 5개 제품 입력
- [ ] 기본 CRUD API 구현
- [ ] 단위 테스트 작성 (커버리지 > 80%)

## Mid-term (1개월 내)
- [ ] 전체 11개 제품 데이터 완료
- [ ] Composition Validation API 구현
- [ ] 제약 조건 엔진 구현
- [ ] 통합 테스트 suite 완성

## Long-term (3개월 내)
- [ ] 학습 모델 훈련 및 배포
- [ ] 프로덕션 환경 출시
- [ ] 모니터링 대시보드 구축
- [ ] 문서화 완료 (API docs, 운영 가이드)
```

---

## 💬 13. Q&A 및 지원

### 자주 묻는 질문

**Q: 왜 계층 구조 대신 그래프 구조를 사용하나요?**
A: Freshdesk Omni처럼 "A + B = C" 형태의 조합 관계와 동적 제약 조건을 표현하려면 그래프가 훨씬 적합합니다. 계층 구조는 상하 관계만 표현 가능하지만, 그래프는 다대다 관계, 순환 관계, 조건부 관계를 모두 표현할 수 있습니다.

**Q: 신규 제품이 추가되면 얼마나 많은 코드를 수정해야 하나요?**
A: 거의 없습니다. 새 ProductModule JSON 파일만 추가하고, 관계 그래프에 엣지를 추가하면 됩니다. 추천 로직은 자동으로 새 제품을 고려합니다.

**Q: 학습 모델 재훈련 주기는 어떻게 되나요?**
A: 초기에는 주 1회 재훈련을 권장합니다. 데이터가 쌓이면 월 1회로 줄이고, 대신 온라인 학습(online learning)을 도입하여 실시간 피드백을 반영합니다.

**Q: 성능이 느려지면 어떻게 최적화하나요?**
A: 3단계 최적화 전략: (1) Redis 캐싱으로 반복 쿼리 가속화, (2) Neo4j 인덱싱 및 쿼리 최적화, (3) 읽기 부하 분산을 위한 Read Replica 추가.

---

## 🎯 결론

본 재설계는 단순히 Freshworks 제품을 분류하는 것을 넘어, **동적 제품 조합, 제약 조건 인식, 비즈니스 시나리오 매칭**을 모두 지원하는 지능형 시스템을 구축하는 것입니다.

**핵심 가치:**
1. **유연성**: 신규 제품/기능 추가 시 최소한의 수정
2. **정확성**: 100% 제약 조건 준수, 잘못된 조합 방지
3. **지능성**: AI 기반 추천으로 고객 니즈 정확히 매칭
4. **확장성**: Freshworks 통합 플랫폼 진화에 선제 대응

**대표님의 비즈니스 철학처럼, 이 시스템도 "흐르는 물처럼" 유연하게 변화에 적응하고, "저울처럼" 정확하게 고객에게 올바른 제품을 추천할 것입니다.**

---

**문서 작성:** AI 컨설턴트  
**최종 검토:** 이우석 대표  
**배포 대상:** 시스템 개발팀  
**버전:** 1.0  
**날짜:** 2025-12-03