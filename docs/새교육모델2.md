계획 수정 완료
젠스파크 제안을 반영하여 핵심 아키텍처를 변경했습니다:
주요 변경사항
1. 제품 구조: 계층 → 그래프
기존: products 테이블 (단순 목록)
변경: product_modules + product_bundles + product_relationships + addon_configurations
2. 핵심 개념
Product Module: 독립 제품 (Freshdesk, Freshchat 등 11개)
Product Bundle: 조합 제품 (Freshdesk Omni = Freshdesk + Freshchat)
Relationship Graph: 제품 간 관계 ('bundles_with', 'addon_to', 'shares_with')
Addon Configuration: 제약 조건 (라이선스 수, 플랜 매칭)
3. 장점
Freshdesk Omni = Freshdesk + Freshchat 관계 명확히 표현
Freshsales → Freshdesk Omni 애드온 제약 조건 선언적 정의
신규 제품 추가 시 구조 변경 최소화
동적 제품 조합 시나리오 지원
4. 커리큘럼 연결
커리큘럼이 독립 제품 OR 번들 제품에 연결됨
번들 전용 학습 콘텐츠 가능 (예: "Omni 통합 워크스페이스")
질문
이 방향으로 진행해도 될까요? 아니면 추가로 고려할 사항이 있나요?
Zenspark 문서의 ML/AI 추천 엔진 부분도 초기 구현에 포함할까요?
아니면 일단 기본 그래프 구조만 구축하고 추천 기능은 나중에 추가할까요?
추천 엔진 부분은 절차상 처음부터 하는게 맞을 것 같아?
맞습니다. 추천 엔진은 나중에 추가하는 것이 좋겠습니다. 이유는:
단계별 접근이 합리적인 이유
Phase 1: 그래프 기반 구조 먼저 (현재 계획)
✅ 제품 모듈/번들/관계 정의
✅ 제약 조건 시스템
✅ 커리큘럼 연결
✅ 학습 진도 추적 → 이것만으로도 충분한 가치 제공
Phase 2: 추천 엔진은 나중에 (3-6개월 후)
학습 데이터가 쌓인 후 (어떤 제품 조합이 인기?, 어떤 시나리오가 많은가?)
실제 사용 패턴 분석
ML 모델 학습 → 데이터 없이 추천은 의미 없음
현재 계획 수정
추천 엔진 관련 부분을 "미래 확장" 섹션으로 옮기고, 초기 구현 범위를 명확히 하겠습니다.
Edit logical-finding-goblet.md
Added 26 lines
Edit logical-finding-goblet.md
Added 34 lines
완벽합니다! 최종 계획을 정리했습니다.
최종 계획 요약
핵심 결정사항
아키텍처: 젠스파크 제안 채택
계층 구조 → 그래프 구조
제품 모듈 + 번들 + 관계 + 제약 조건
구현 범위: 단계별 접근
Phase 1 (7주): MVP - 그래프 기반 학습 시스템
Phase 2 (3-6개월 후): ML 추천 엔진 (데이터 쌓인 후)
제품 구조 (11개 모듈 + 2개 번들)
독립 모듈: Freshdesk, Freshchat, Freshsales, Freshservice 등
통합 번들: Freshdesk Omni, Freshsales Suite
관계 그래프: bundles_with, addon_to, shares_with
학습 철학:
평가 X → 학습 보조 O
통과/불통과 X → 완료/미완료 O
레벨 잠금 X → 권장 순서 O
다음 단계
계획이 완성되었습니다. 실행 준비가 되었으면 말씀해주세요!
Claude’s Plan
온보딩 시스템 전면 재설계 계획
목표
평가/테스트 중심 시스템 → 학습 보조 시스템으로 전환
정답 찾기 ❌ → 지식 내재화 돕기 ✅
통과/불통과 판정 제거
대표의 주관적 판단 지원
고품질 커리큘럼 (RAG → DB 점진적 큐레이션)
핵심 설계 원칙
1. 학습 경로
순차 학습 권장 (잠금 해제 ❌)
모든 레벨/모듈 처음부터 열림
"권장 순서" 안내만 제공
2. 퀴즈 재정의
평가 도구 ❌ → 자가 점검 도구 ✅
점수는 참고용 (통과 기준 없음)
모든 문제 해설 제공
부족한 부분 인지 목적
3. 하이브리드 콘텐츠 아키텍처
Layer 2: Supabase Postgres (고품질 큐레이션)
         ↓ (보충)
Layer 1: Google RAG Store (유연한 실시간 생성)
Phase 1: 평가 기능 제거
제거 대상
1. 페이지 파일
pages/AssessmentPage.tsx
pages/WorkSenseAssessmentPage.tsx
pages/ProductKnowledgeLevelsPage.tsx
pages/ProductKnowledgeLevelPage.tsx
2. 라우트 (App.tsx)
// 제거
<Route path="/assessment" element={<AssessmentPage />} />
<Route path="/assessment/:trackId" element={<WorkSenseAssessmentPage />} />
<Route path="/assessment/:trackId/levels" element={<ProductKnowledgeLevelsPage />} />
<Route path="/assessment/:trackId/level/:levelId" element={<ProductKnowledgeLevelPage />} />
3. API 함수 (apiClient.ts)
// 제거할 함수들 (라인 483-703)
- getAssessmentTracks()
- getAssessmentLevels()
- streamLearningContent()
- streamMentorChat() (Assessment용)
- getAssessmentQuestions()
- submitAssessment()
- getAssessmentProgress()
4. 타입 정의 (types.ts)
// 제거
- AssessmentTrack
- AssessmentLevel
- AssessmentQuestion
- AssessmentSubmitRequest
- AssessmentResult
5. 사이드바 메뉴 (Sidebar.tsx)
// 제거
{ path: '/assessment', icon: 'fas fa-clipboard-check', label: '학습 평가' }
Phase 2: 데이터베이스 재설계
핵심 아키텍처 변경 (Zenspark 제안 반영)
문제: 기존 계층적 제품 분류 방식의 한계
Freshdesk Omni = Freshdesk + Freshchat 관계 표현 불가
Add-on 제약 조건 누락 (라이선스 수, 플랜 매칭)
동적 제품 조합 시나리오 지원 불가
해결: 모듈 기반 조합 아키텍처
계층 구조 → 그래프 구조
독립 제품을 모듈로 정의
번들 제품을 "모듈 조합 + 통합 레이어"로 표현
제약 조건을 메타데이터로 선언적 정의
Supabase 현황
기존 테이블: onboarding_scenarios, onboarding_sessions, onboarding_progress, curriculum_modules, quiz_questions, quiz_attempts
전략: 기존 테이블 버리고 새로 설계 (혼선 방지)
제품 정보 (그래프 기반)
독립 모듈 (11개):
Customer Service: Freshdesk, Freshchat, Freshcaller
Sales & Marketing: Freshsales, Freshmarketer
ITSM: Freshservice, Freshstatus
기타: Freshteam, Freshping, Freshconnect, Freshrelease
통합 번들 (2개):
Freshdesk Omni = Freshdesk + Freshchat (native unified)
Freshsales Suite = Freshsales + 기타 (connected suite)
Add-on 관계:
Freshsales → Freshdesk Omni (제약: min=admins, max=licenses, plan_matching)
Freshcaller → Freshdesk Omni / Freshsales (shared usage)
RAG Store: 6000여건 문서 (product 파라미터로 구분)
새 스키마 설계 (그래프 기반)
1. Product Module (독립 제품 모듈)
CREATE TABLE product_modules (
  id VARCHAR(50) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  name_ko VARCHAR(100) NOT NULL,

  -- 도메인 분류
  domain VARCHAR(50) NOT NULL,
    -- 'customer_service', 'sales_marketing', 'itsm', 'other'

  -- 제품 특성
  standalone BOOLEAN DEFAULT true,  -- 독립 사용 가능 여부
  capabilities JSONB NOT NULL,      -- [{"id": "ticketing", "description": "...", "freddy_ai_enabled": true}]

  -- 가격 정보
  pricing_info JSONB,
    -- {"free_tier": true, "starting_price": 15, "currency": "USD", "billing_unit": "agent/month"}

  -- RAG 연동
  rag_product_keys TEXT[],          -- ['freshdesk', 'freshdesk-omni']

  -- 메타데이터
  icon VARCHAR(50),
  color VARCHAR(20),
  display_order INT DEFAULT 0,
  is_active BOOLEAN DEFAULT true,

  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 도메인 인덱스
CREATE INDEX idx_product_modules_domain ON product_modules(domain);

-- 초기 데이터 (11개 제품)
INSERT INTO product_modules (id, name, name_ko, domain, standalone, capabilities, rag_product_keys) VALUES
  -- Customer Service Domain
  ('freshdesk', 'Freshdesk', 'Freshdesk', 'customer_service', true,
   '[{"id":"ticketing","description":"Multi-channel ticket management","freddy_ai_enabled":true}]'::jsonb,
   ARRAY['freshdesk']),

  ('freshchat', 'Freshchat', 'Freshchat', 'customer_service', true,
   '[{"id":"live_chat","description":"Real-time customer chat","freddy_ai_enabled":true}]'::jsonb,
   ARRAY['freshchat']),

  ('freshcaller', 'Freshcaller', 'Freshcaller', 'customer_service', true,
   '[{"id":"voice","description":"Cloud phone system","freddy_ai_enabled":false}]'::jsonb,
   ARRAY['freshcaller']),

  -- Sales & Marketing Domain
  ('freshsales', 'Freshsales', 'Freshsales', 'sales_marketing', true,
   '[{"id":"crm","description":"Sales CRM platform","freddy_ai_enabled":true}]'::jsonb,
   ARRAY['freshsales']),

  -- ITSM Domain
  ('freshservice', 'Freshservice', 'Freshservice', 'itsm', true,
   '[{"id":"itsm","description":"IT service management","freddy_ai_enabled":true}]'::jsonb,
   ARRAY['freshservice']);
2. Product Bundle (통합 번들)
CREATE TABLE product_bundles (
  id VARCHAR(50) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  name_ko VARCHAR(100) NOT NULL,

  -- 번들 구성
  base_module_ids TEXT[] NOT NULL,  -- ['freshdesk', 'freshchat']

  -- 통합 특성
  integration_type VARCHAR(20) NOT NULL,
    -- 'native_unified' (Freshdesk Omni 스타일)
    -- 'connected_suite' (Freshsales Suite 스타일)

  workspace_type VARCHAR(20) NOT NULL,
    -- 'unified', 'integrated', 'connected'

  -- 가격 정보
  pricing_info JSONB,
  pricing_advantage DECIMAL(5,2),   -- 번들 할인율 (0.20 = 20%)

  -- 가치 제안
  value_propositions TEXT[],
    -- ['Single agent workspace', 'Unified customer context']

  -- RAG 연동
  rag_product_keys TEXT[],

  -- 메타데이터
  icon VARCHAR(50),
  color VARCHAR(20),
  display_order INT DEFAULT 0,
  is_active BOOLEAN DEFAULT true,

  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 초기 데이터
INSERT INTO product_bundles (id, name, name_ko, base_module_ids, integration_type, workspace_type, rag_product_keys) VALUES
  ('freshdesk_omni', 'Freshdesk Omni', 'Freshdesk Omni',
   ARRAY['freshdesk', 'freshchat'],
   'native_unified',
   'unified',
   ARRAY['freshdesk-omni']);
3. Product Relationship (제품 관계 그래프)
CREATE TABLE product_relationships (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  source_id VARCHAR(50) NOT NULL,   -- product_modules.id or product_bundles.id
  target_id VARCHAR(50) NOT NULL,

  relationship_type VARCHAR(20) NOT NULL,
    -- 'bundles_with': A + B → Bundle
    -- 'addon_to': A → B (A는 B의 애드온)
    -- 'shares_with': A ↔ B (양방향 공유)
    -- 'integrates_with': API 기반 통합
    -- 'replaces': 상위 플랜 대체

  bidirectional BOOLEAN DEFAULT false,

  -- 통합 특성 (bundles_with, addon_to용)
  integration_characteristics JSONB,
    -- {"conversation_to_ticket": true, "shared_contact_db": true, "context_preservation": "full"}

  -- 결과 번들 (bundles_with용)
  result_bundle_id VARCHAR(50),     -- REFERENCES product_bundles(id)

  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_relationships_source ON product_relationships(source_id);
CREATE INDEX idx_relationships_target ON product_relationships(target_id);
CREATE INDEX idx_relationships_type ON product_relationships(relationship_type);

-- 초기 관계 데이터
INSERT INTO product_relationships (source_id, target_id, relationship_type, result_bundle_id, integration_characteristics) VALUES
  -- Freshdesk + Freshchat → Freshdesk Omni
  ('freshdesk', 'freshchat', 'bundles_with', 'freshdesk_omni',
   '{"conversation_to_ticket": true, "shared_contact_db": true, "unified_workspace": true, "context_preservation": "full"}'::jsonb);
4. Addon Configuration (애드온 제약 조건)
CREATE TABLE addon_configurations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  addon_module_id VARCHAR(50) NOT NULL,      -- REFERENCES product_modules(id)
  target_bundle_id VARCHAR(50) NOT NULL,     -- REFERENCES product_bundles(id)

  integration_depth VARCHAR(20) NOT NULL,
    -- 'deep': 깊은 통합 (데이터 공유, 워크플로우 통합)
    -- 'shared': 공유 사용 (동일 리소스 활용)
    -- 'lightweight': 가벼운 통합 (API 연동)

  -- 제약 조건 배열
  constraints JSONB NOT NULL,
    -- [
    --   {"type": "license_quantity", "rule": {"min_seats": "num_of_admins", "max_seats": "css_plan_licenses"}},
    --   {"type": "plan_tier", "rule": {"plan_matching": "exact"}}
    -- ]

  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_addon_configs_target ON addon_configurations(target_bundle_id);

-- 초기 데이터 (Freshsales → Freshdesk Omni 제약)
INSERT INTO addon_configurations (addon_module_id, target_bundle_id, integration_depth, constraints) VALUES
  ('freshsales', 'freshdesk_omni', 'deep',
   '[
     {"type": "license_quantity", "rule": {"min_seats": "num_of_admins", "max_seats": "css_plan_licenses", "expression": "addon_seats <= base_licenses && addon_seats >= admin_count"}},
     {"type": "plan_tier", "rule": {"plan_matching": "exact", "expression": "addon_plan_tier == base_plan_tier"}}
   ]'::jsonb);
5. 커리큘럼 모듈 (학습 경로)
CREATE TABLE curriculum_modules (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  -- 제품 연결 (product_modules OR product_bundles)
  target_product_id VARCHAR(50) NOT NULL,
  target_product_type VARCHAR(20) NOT NULL,
    -- 'module' (독립 제품), 'bundle' (통합 번들)

  name_ko VARCHAR(200) NOT NULL,
  name_en VARCHAR(200),
  slug VARCHAR(100) NOT NULL,
  description TEXT,
  icon VARCHAR(50),

  -- 권장 순서 (강제 아님)
  display_order INT DEFAULT 0,

  -- 학습 정보
  learning_objectives JSONB,  -- ['목표1', '목표2']
  estimated_minutes INT,

  -- 콘텐츠 전략
  content_strategy VARCHAR(20) DEFAULT 'hybrid',  -- 'db_only', 'rag_only', 'hybrid'

  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),

  UNIQUE(target_product_id, slug)
);

CREATE INDEX idx_curriculum_modules_target ON curriculum_modules(target_product_id);

-- 예시: Freshservice 커리큘럼
INSERT INTO curriculum_modules (target_product_id, target_product_type, name_ko, slug, display_order) VALUES
  ('freshservice', 'module', '티켓 관리 기초', 'ticket-basics', 1),
  ('freshservice', 'module', '자동화 및 워크플로우', 'automation', 2),
  ('freshservice', 'module', '에이전트 협업', 'collaboration', 3);

-- 예시: Freshdesk Omni 커리큘럼 (번들 전용 학습)
INSERT INTO curriculum_modules (target_product_id, target_product_type, name_ko, slug, display_order) VALUES
  ('freshdesk_omni', 'bundle', 'Omni 통합 워크스페이스', 'omni-workspace', 1),
  ('freshdesk_omni', 'bundle', '채팅-티켓 전환 플로우', 'chat-to-ticket', 2);
3. 섹션 콘텐츠 (중레벨)
CREATE TABLE curriculum_sections (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  module_id UUID REFERENCES curriculum_modules(id) ON DELETE CASCADE,

  section_type VARCHAR(20) NOT NULL,  -- 'overview', 'features', 'practice', 'faq'
  display_order INT DEFAULT 0,
  title_ko VARCHAR(200),
  title_en VARCHAR(200),

  -- 큐레이션된 콘텐츠 (고품질)
  curated_content TEXT,  -- 마크다운
  curated_version INT DEFAULT 1,
  is_verified BOOLEAN DEFAULT false,
  last_curated_by VARCHAR(100),
  last_curated_at TIMESTAMPTZ,

  -- RAG 보조
  rag_query_hint TEXT,
  fallback_to_rag BOOLEAN DEFAULT true,

  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),

  UNIQUE(module_id, section_type)
);

CREATE INDEX idx_curriculum_sections_module ON curriculum_sections(module_id);
4. 퀴즈 문제 (자가 점검용)
CREATE TABLE quiz_questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  module_id UUID REFERENCES curriculum_modules(id) ON DELETE CASCADE,

  difficulty VARCHAR(20) NOT NULL,  -- 'basic', 'advanced'
  question_order INT DEFAULT 0,

  question TEXT NOT NULL,
  context TEXT,  -- 배경 설명
  choices JSONB NOT NULL,  -- [{"id": "a", "text": "선택지1"}, ...]
  correct_choice_id VARCHAR(10) NOT NULL,

  -- 학습 중심 해설 (평가 X)
  explanation TEXT NOT NULL,
  learning_point TEXT,  -- "이 문제를 통해 배울 점"
  related_doc_url TEXT,

  -- 큐레이션 정보
  quality_rating INT CHECK (quality_rating BETWEEN 1 AND 5),
  is_verified BOOLEAN DEFAULT false,
  reviewed_by VARCHAR(100),
  reviewed_at TIMESTAMPTZ,

  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_quiz_questions_module_difficulty
  ON quiz_questions(module_id, difficulty);
5. 세션 관리
CREATE TABLE learning_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(100) UNIQUE NOT NULL,  -- 클라이언트 UUID
  user_email VARCHAR(255),
  user_name VARCHAR(100),

  created_at TIMESTAMPTZ DEFAULT NOW(),
  last_active_at TIMESTAMPTZ DEFAULT NOW(),

  -- 메타데이터
  user_agent TEXT,
  ip_address INET
);

CREATE INDEX idx_learning_sessions_session_id ON learning_sessions(session_id);
6. 모듈 진도 추적
CREATE TABLE module_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(100) REFERENCES learning_sessions(session_id),
  module_id UUID REFERENCES curriculum_modules(id),

  -- 상태 (평가 개념 제거)
  status VARCHAR(20) DEFAULT 'not_started',
    -- 'not_started', 'learning', 'completed'

  -- 학습 시간
  learning_started_at TIMESTAMPTZ,
  learning_completed_at TIMESTAMPTZ,
  total_time_seconds INT DEFAULT 0,

  -- 퀴즈 시도 (참고용, 통과 기준 없음)
  basic_quiz_attempted BOOLEAN DEFAULT false,
  basic_quiz_score INT,  -- 참고용
  basic_quiz_attempts INT DEFAULT 0,

  advanced_quiz_attempted BOOLEAN DEFAULT false,
  advanced_quiz_score INT,  -- 참고용
  advanced_quiz_attempts INT DEFAULT 0,

  -- 완료 기준: 학습 + 퀴즈 1회 이상 시도 (점수 무관)
  completed_at TIMESTAMPTZ,

  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),

  UNIQUE(session_id, module_id)
);

CREATE INDEX idx_module_progress_session
  ON module_progress(session_id);
7. 퀴즈 시도 기록
CREATE TABLE quiz_attempts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(100) REFERENCES learning_sessions(session_id),
  module_id UUID REFERENCES curriculum_modules(id),

  difficulty VARCHAR(20) NOT NULL,

  -- 답변 및 결과
  answers JSONB NOT NULL,
    -- [{"questionId": "uuid", "choiceId": "a", "isCorrect": true}]
  score INT NOT NULL,  -- 참고용
  total_questions INT NOT NULL,
  correct_count INT NOT NULL,

  -- 시간 정보
  started_at TIMESTAMPTZ NOT NULL,
  completed_at TIMESTAMPTZ NOT NULL,
  duration_seconds INT,

  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_quiz_attempts_session_module
  ON quiz_attempts(session_id, module_id);
8. AI 생성 콘텐츠 로그 (저레벨 → 고레벨 후보)
CREATE TABLE ai_generated_content (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(100),
  module_id UUID REFERENCES curriculum_modules(id),
  section_id UUID REFERENCES curriculum_sections(id),

  -- 생성 맥락
  context_type VARCHAR(50),  -- 'section_content', 'chat_response', 'quiz_explanation'
  user_query TEXT,
  prompt TEXT,

  -- AI 응답
  response TEXT NOT NULL,
  source VARCHAR(20) NOT NULL,  -- 'rag', 'gemini'
  model VARCHAR(50),
  tokens_used INT,

  -- 큐레이션
  quality_rating INT CHECK (quality_rating BETWEEN 1 AND 5),
  is_useful BOOLEAN,
  admin_notes TEXT,
  promoted_to_section_id UUID REFERENCES curriculum_sections(id),

  generated_at TIMESTAMPTZ DEFAULT NOW(),
  reviewed_at TIMESTAMPTZ,
  reviewed_by VARCHAR(100)
);

CREATE INDEX idx_ai_content_quality
  ON ai_generated_content(quality_rating DESC, generated_at DESC)
  WHERE quality_rating IS NOT NULL;
9. 대표 검토/피드백
CREATE TABLE admin_reviews (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(100) REFERENCES learning_sessions(session_id),
  module_id UUID REFERENCES curriculum_modules(id),

  reviewer_email VARCHAR(255) NOT NULL,
  review_type VARCHAR(20) NOT NULL,  -- 'module_complete', '1on1_feedback', 'general'

  -- 주관적 평가
  comments TEXT NOT NULL,
  rating INT CHECK (rating BETWEEN 1 AND 5),  -- 선택적
  strengths TEXT,  -- 잘한 점
  improvements TEXT,  -- 개선 필요한 점
  next_steps TEXT,  -- 다음 단계 제안

  -- 상태
  status VARCHAR(20) DEFAULT 'draft',  -- 'draft', 'shared'
  shared_at TIMESTAMPTZ,
  employee_acknowledged_at TIMESTAMPTZ,

  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_admin_reviews_session
  ON admin_reviews(session_id, created_at DESC);
Phase 3: 프론트엔드 재구성
1. 새로운 학습 페이지 구조
A. 제품 선택 페이지
파일: pages/ProductSelectionPage.tsx (NEW)
// 4개 제품 카드 표시
// - Freshservice (권장 시작점)
// - Freshdesk
// - Freshchat
// - Freshsales
B. 커리큘럼 목록 페이지
파일: pages/CurriculumModulesPage.tsx (수정)
// 변경사항:
// 1. 레벨 잠금 제거 (isUnlocked 체크 삭제)
// 2. "권장 순서" 배지 추가
// 3. 상태: 미시작 / 학습 중 / 완료 (통과/불통과 제거)
// 4. 진행률 표시: 학습 완료 + 퀴즈 시도 여부
C. 모듈 학습 페이지
파일: pages/ModuleLearningPage.tsx (수정)
// 변경사항:
// 1. 콘텐츠 로드 우선순위
//    - Postgres 큐레이션 콘텐츠 우선
//    - 없으면 RAG 실시간 생성
//    - 로그 저장 (큐레이션 후보)
//
// 2. 퀴즈 재정의
//    - "자가 점검" 타이틀
//    - 점수는 참고용 표시
//    - "이 문제로 배울 점" 강조
//    - 통과/불통과 메시지 제거
//    - 모든 문제 해설 표시
//
// 3. 완료 조건
//    - 학습 완료 + 퀴즈 1회 시도 (점수 무관)
//    - "다음 모듈로" 버튼 항상 활성화
D. 대표 대시보드
파일: pages/AdminDashboardPage.tsx (NEW)
// 기능:
// 1. 직원별 학습 현황
//    - 완료한 모듈 목록
//    - 학습 소요 시간
//    - 퀴즈 시도 횟수 (점수 아님)
//
// 2. AI 생성 콘텐츠 검토
//    - 품질 좋은 응답 필터링
//    - 별점 부여
//    - "커리큘럼에 추가" 버튼
//
// 3. 피드백 작성
//    - 직원별 코멘트
//    - 잘한 점 / 개선점 / 다음 단계
//    - 주관적 평가
2. UI/UX 변경사항
진행 표시
Before (평가 중심):
✓ Level 1: 시장 포지셔닝 (85% 통과)
🔒 Level 2: 설계 철학 (잠김 - 이전 레벨 80% 이상 필요)
After (학습 중심):
✓ 1단계: 시장 포지셔닝 (완료)
📍 2단계: 설계 철학 (권장 다음 단계)
  3단계: 핵심 기능
  4단계: 세부 기능
퀴즈 결과 화면
Before:
[초록색 트로피]
축하합니다! 이 레벨을 통과했습니다.
점수: 85%
다음 레벨이 해제됩니다!
After:
[체크 아이콘]
학습 확인 완료
점수: 85% (참고용)

📚 복습이 필요한 주제:
- 질문 2: 티켓 우선순위 설정 (해설 보기)
- 질문 5: 자동화 규칙 (해설 보기)

[다음 모듈로] [다시 풀어보기]
Phase 4: 백엔드 API 수정
1. 새로운 엔드포인트
제품 관리
GET /api/products
  → 4개 제품 목록

GET /api/products/:productId/modules
  → 제품별 커리큘럼 모듈 목록 (권장 순서 포함)
콘텐츠 제공 (하이브리드)
GET /api/curriculum/modules/:moduleId/section/:sectionId/content
  → 1. Postgres 큐레이션 콘텐츠 확인
  → 2. 없으면 RAG 생성
  → 3. ai_generated_content에 로그

Response:
{
  "source": "database" | "rag",
  "content": "마크다운 콘텐츠",
  "quality": "high" | "generated"
}
퀴즈 (평가 개념 제거)
POST /api/curriculum/modules/:moduleId/quiz/submit
Request:
{
  "sessionId": "uuid",
  "difficulty": "basic" | "advanced",
  "answers": [{"questionId": "uuid", "choiceId": "a"}]
}

Response:
{
  "score": 85,
  "totalQuestions": 10,
  "correctCount": 8,
  "isPassed": false,  // 항상 false (평가 개념 제거)
  "answers": [
    {
      "questionId": "uuid",
      "choiceId": "a",
      "isCorrect": true,
      "correctChoiceId": "a",
      "explanation": "...",
      "learningPoint": "..."  // 추가
    }
  ]
}
관리자 기능
GET /api/admin/ai-content
  → 품질 좋은 AI 생성 콘텐츠 조회

POST /api/admin/ai-content/:id/promote
  → DB로 승격 (큐레이션)

POST /api/admin/reviews
  → 직원 피드백 작성
2. 제거할 엔드포인트
DELETE /api/assessment/*  (전체 삭제)
Phase 5: 마이그레이션 전략
1. 데이터베이스
-- 1. 기존 테이블 백업
CREATE TABLE _backup_curriculum_modules AS
  SELECT * FROM curriculum_modules;

-- 2. 기존 테이블 삭제
DROP TABLE IF EXISTS
  onboarding_scenarios,
  onboarding_progress,
  quiz_attempts,
  quiz_questions,
  curriculum_modules,
  onboarding_sessions
CASCADE;

-- 3. 새 스키마 생성 (위 Phase 2 스키마)

-- 4. 기본 데이터 입력
-- products
-- curriculum_modules (Freshservice 먼저)
2. 코드베이스
1. 평가 관련 파일 삭제
2. 새 페이지 생성
3. 라우팅 수정
4. API 함수 수정
5. 타입 정의 수정
Phase 6: 구현 순서 (MVP 중심)
🎯 MVP 범위 정의
포함:
✅ 그래프 기반 제품 구조
✅ 제약 조건 시스템
✅ 하이브리드 콘텐츠 (RAG + DB)
✅ 학습 진도 추적
✅ 대표 검토 시스템
제외 (나중에):
⏳ ML 추천 엔진
⏳ 비즈니스 시나리오 자동 매칭
⏳ 제품 조합 최적화 알고리즘
Week 1: 기반 구조
 그래프 기반 DB 스키마 생성
product_modules (11개 제품)
product_bundles (Freshdesk Omni)
product_relationships
addon_configurations
 기존 테이블 백업 및 삭제
 평가 관련 코드 제거 (페이지, 라우트, API)
Week 2: 제품 그래프 구축
 11개 제품 모듈 데이터 입력
 Freshdesk Omni 번들 정의
 제품 관계 그래프 구축
 Freshsales → Omni 제약 조건 설정
 제품 조회 API 구현
Week 3: 커리큘럼 재설계
 커리큘럼 모듈 재구조화 (target_product_id/type)
 Freshservice 커리큘럼 우선 구축
 하이브리드 콘텐츠 로직 (DB 우선 → RAG 폴백)
 AI 생성 로그 저장
Week 4: 퀴즈 재설계
 퀴즈 문제 DB 이관
 평가 개념 제거 (isPassed 제거)
 학습 포인트 강조 UI
 모든 문제 해설 표시
 "자가 점검" 메시지로 변경
Week 5: 프론트엔드 재구성
 제품 선택 페이지 (도메인별 그룹핑)
 커리큘럼 목록 (레벨 잠금 제거)
 학습 페이지 (권장 순서 표시)
 진행률 UI (통과/불통과 → 완료/미완료)
Week 6: 관리자 기능
 직원별 학습 현황 대시보드
 AI 콘텐츠 검토 UI
 피드백 작성 기능
 큐레이션 승격 기능
Week 7: 테스트 & 출시
 E2E 테스트
 제품 그래프 검증
 성능 최적화
 문서화
 배포
핵심 파일 목록
삭제 대상
pages/AssessmentPage.tsx
pages/WorkSenseAssessmentPage.tsx
pages/ProductKnowledgeLevelsPage.tsx
pages/ProductKnowledgeLevelPage.tsx
수정 대상
pages/CurriculumModulesPage.tsx
pages/ModuleLearningPage.tsx
services/apiClient.ts (라인 483-703 제거)
types.ts (Assessment 타입 제거)
App.tsx (라우팅)
components/layout/Sidebar.tsx (메뉴)
신규 생성
pages/ProductSelectionPage.tsx
pages/AdminDashboardPage.tsx
components/admin/AIContentReview.tsx
components/admin/EmployeeFeedbackForm.tsx
components/learning/ContentViewer.tsx (DB/RAG 구분)
성공 지표
학습 효과
평균 학습 완료율 (현재 vs 재설계 후)
모듈당 평균 소요 시간
AI 멘토 질문 빈도 (참여도)
콘텐츠 품질
DB 큐레이션 콘텐츠 비율 (목표: 3개월 내 80%)
AI 생성 콘텐츠 품질 평점 추이
RAG 호출 비용 절감률
대표 만족도
피드백 작성 빈도
1:1 면담 시 시스템 활용도
주관적 판단에 필요한 정보 충분성
리스크 & 대응
리스크 1: 콘텐츠 부족
문제: 초기에 DB 콘텐츠가 없어 모두 RAG 의존 대응: Freshservice는 수동으로 1개 모듈 큐레이션 후 시작
리스크 2: 학습자 방향성 상실
문제: 잠금 해제 없으면 어디서부터 시작할지 모름 대응: "권장 학습 경로" 명확히 표시, 대시보드에 진행률
리스크 3: 퀴즈 동기부여 저하
문제: 통과 기준 없으면 퀴즈를 안 풀 수 있음 대응: "자가 점검 완료" 배지, 대표 검토 시 참고 자료로 활용
다음 단계
✅ 계획 승인
DB 스키마 생성 스크립트 작성
평가 관련 코드 제거 PR
새 페이지 프로토타입
백엔드 API 개발
미래 확장 (Phase 2: 3-6개월 후)
ML/AI 추천 엔진 구축
전제 조건: 충분한 학습 데이터 축적
사용자별 학습 패턴 (선호 제품, 완료율)
제품 조합 선택 통계
비즈니스 시나리오별 성공 사례
구현 항목:
제품 임베딩
Sentence Transformers로 제품 설명 벡터화
Node2Vec로 제품 관계 그래프 임베딩
시나리오 기반 추천
회사 프로필 → 제품 조합 추천
LightGBM 랭킹 모델
비용 최적화 엔진
제약 조건 검증 자동화
Constraint Satisfaction Problem (CSP)
python-constraint 라이브러리
실시간 유효성 검증
마이그레이션 경로 생성
현재 도구 → Freshworks 전환 시나리오
단계별 구현 계획 자동 생성
참고: 젠스파크 문서의 Phase 2-4 내용 참조
최종 목표:
Phase 1 (MVP): 신입사원이 부담 없이 학습하고, 대표가 주관적으로 성장을 판단할 수 있는 시스템
Phase 2 (확장): AI 기반 제품 추천 및 최적화로 영업/컨설팅 자동화